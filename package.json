{
  "name": "fake-news-detector-extension",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run watch-extension & npm run watch-elm",
    "watch-extension":
      "web-ext run --verbose --ignore-files elm-stuff '*.js' node_modules 'src/*.elm'",
    "watch-elm":
      "chokidar 'src/*.elm' -c 'elm make src/StoryVotes.elm --output dist/StoryVotes.js && elm make src/FlagPopup.elm --output dist/FlagPopup.js' --initial"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "chokidar-cli": "^1.2.0",
    "elm": "^0.18.0",
    "web-ext": "^2.0.0"
  }
}
